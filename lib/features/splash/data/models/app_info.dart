/// This class holds the data info such as package name version and so on
class AppInfo {
  String appName;
  String packageName;
  String version;
  String buildNumber;

  AppInfo(this.appName, this.packageName, this.version, this.buildNumber);
}
